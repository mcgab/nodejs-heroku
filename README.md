## NodeJS / Mongo example project

Project can be deployed on Heroku.

To run in dev enviroment run:
- docker-compose up -d
- npm install
- set up database connection in .env (exmaple in .env.example)
- npm run dev



